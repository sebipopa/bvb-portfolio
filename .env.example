# Optional: URL of your deployed price worker (see worker/README.md).
# Leave empty and the app fetches quotes directly from TradingView/Yahoo.
# Restart `expo start` after changing this file.
EXPO_PUBLIC_PRICE_API_URL=
